#include "util.h"
#include "tens.h"

void softmax_forward(ftens *t)
{
     fprintf(stderr, "not implemeted yet\n");
     exit(-4);
}


void softmax_backward(ftens *dout)
{
     fprintf(stderr, "not implemeted yet\n");
     exit(-4);
}
